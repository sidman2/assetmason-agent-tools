# JSON Output Proof

- `ard-cli check ... --json` returned a single JSON object and parsed cleanly.
- `ai-catalog validate ... --json` returned a single JSON object and parsed cleanly.
- `ai-catalog --version` and `ai-catalog --help` currently emit structured JSON in this preview implementation.

The JSON outputs contain structured data only and no marketing prose outside the fields.

